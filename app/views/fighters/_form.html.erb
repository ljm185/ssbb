<%= form_with(model: fighter, local: true) do |form| %>
  <% if fighter.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(fighter.errors.count, "error") %> prohibited this fighter from being saved:</h2>

      <ul>
      <% fighter.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :catchphrase %>
    <%= form.text_field :catchphrase %>
  </div>

  <div class="field">
    <%= form.label :clone %>
    <%= form.check_box :clone %>
  </div>

  <div class="field">
    <%= form.label :series_id %>
    <%= form.text_field :series_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
